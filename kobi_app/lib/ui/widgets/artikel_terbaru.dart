part of 'widgets.dart';

class RecentArticle extends StatelessWidget {
  final Artikel artikel;
  final Function onTap;

  RecentArticle(this.artikel, {this.onTap});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
